services:
  qwen-vllm:
    devices:
      - "nvidia.com/gpu=all"
    security_opt:
      - "label=disable"
    ipc: host
    environment:
      NVIDIA_VISIBLE_DEVICES: all
      NVIDIA_DRIVER_CAPABILITIES: compute,utility
